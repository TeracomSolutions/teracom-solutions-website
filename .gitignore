# dependencies
/node_modules

# next.js build output
/.next/
/out/

# local env files
.env
.env.local
.env.*.local

# misc
.DS_Store
*.log

# typescript (jsconfig.json is what this repo actually uses — this repo is
# JS, not TS; these two are ignored pre-emptively in case @types/node or
# @types/react ever trigger Next.js's own auto-generated tsconfig.json)
*.tsbuildinfo
next-env.d.ts
