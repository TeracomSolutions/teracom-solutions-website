import Image from 'next/image';
import Link from 'next/link';
import CheckoutButton from '@/components/CheckoutButton';
import {products,formatMoney} from '@/lib/products';
import {categories} from '@/lib/categories';
export default function Store(){return <main><section className="hero hero-product"><div className="container hero-layout"><div className="hero-copy"><span className="eyebrow">Teracom Store</span><h1>Security products, software and industry resources.</h1><p className="lead">A marketplace connecting security professionals with selected products, digital resources, training and SecurityOS subscriptions.</p></div><div className="hero-image"><Image src="/assets/store-preview.svg" alt="Teracom Store preview" width={1200} height={700}/></div></div></section><section className="section section-spacious"><div className="container"><div className="section-heading"><span className="eyebrow">Browse by category</span><h2>Shop by product category.</h2><p>Full catalogues are being rebuilt category by category -- browse what&apos;s here now, more stock and pricing is on its way.</p></div><div className="feature-grid">{categories.map(c=><Link href={`/store/${c.slug}`} key={c.slug} style={{textDecoration:'none',color:'inherit'}}><article><h3>{c.title}</h3><p>{c.description}</p></article></Link>)}</div></div></section><section className="section section-spacious alt"><div className="container"><div className="section-heading"><span className="eyebrow">Products & subscriptions</span><h2>A practical store for the electronic security industry.</h2><p>Start with subscriptions, digital products and services. Add supplier-fed hardware products as data feeds become available.</p></div><div className="product-grid">{products.map(p=><article className="product-card" key={p.id}><div><span className="badge">{p.type}</span><h3>{p.name}</h3><p>{p.description}</p></div><div><p className="price">{formatMoney(p.priceCents)}</p><CheckoutButton productId={p.id} label={p.type==='subscription'?'Subscribe':'Buy'} /></div></article>)}</div></div></section></main>}
